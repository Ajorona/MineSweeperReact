import React from 'react';

import spinner from '../../../assets/images/ripple.gif';

const Spinner = (props) => (
    <div>
        <img src={spinner} alt="where am I??" />
    </div>
);

export default Spinner;
